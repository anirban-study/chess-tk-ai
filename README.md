# Tk Chess — Human (White) vs Computer (Black)

A Tkinter chess app using `python-chess`.  
You play White; the computer plays Black (alpha-beta engine with depth control).

## Requirements
- Python 3.8+
- `python-chess`

## Install & Run
```bash
pip install python-chess
python tk_chess_vs_ai.py
